apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
metadata:
  name: summitseekers
resources:
  - backend.yaml
  - dashboard.yaml
  # - db.yaml  # Commented out - using DigitalOcean Managed PostgreSQL instead
  # Secrets are applied manually via apply-secrets.sh (not in Git)
  - frontend.yaml
  - ingress.yaml
  - cloudflared.yaml
