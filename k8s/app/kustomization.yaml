apiVersion: kustomize.config.k8s.io/v1beta2
kind: Kustomization
namespace: default
metadata:
  name: run-goals
resources:
  - backend.yaml
  - dashboard.yaml
  - db.yaml
  - frontend.yaml
  - ingress.yaml
