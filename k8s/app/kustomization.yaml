apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
metadata:
  name: summitseekers
resources:
  - backend.yaml
  - dashboard.yaml
  # - db.yaml  # Commented out - using DigitalOcean Managed PostgreSQL instead
  - db-secret.yaml
  - frontend.yaml
  - ingress.yaml
  - cloudflared.yaml
